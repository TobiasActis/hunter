# Copiá este archivo a .env (o seteá estas variables en tu entorno) y
# completá los valores reales -- .env nunca se sube a git (ver
# .gitignore). Sin esto, el dashboard usa una contraseña placeholder
# insegura (ver config/settings.py).

DASHBOARD_USERNAME=tobias
DASHBOARD_PASSWORD=
